<div
    class="min-h-screen flex items-center justify-center bg-[#f1f6fb]"
>
    <div class="w-full max-w-md">
        <div class="text-center mb-5 flex flex-row gap-5 justify-center ">
            <div
                class="inline-flex items-center justify-center w-16 h-16 gradient-primary mb-4 rounded-2xl shadow-elegant"
_           >
                <lucide-angular [img]="Building2" class="h-8 w-8 text-white" />
            </div>
            <div>
                <h1 class="text-3xl font-bold">Property Manager</h1>
                <p class="text-muted-foreground">Gestão inteligente de propriedades</p>
            </div>
        </div>
        <div class="shadow-elegant rounded-2xl p-5 bg-white">  
            <div class="mb-5">
                <h2 class="font-semibold text-2xl">Bem-vindo</h2>
                <p class="text-sm text-gray-500">Entre ou crie sua conta para continuar</p>
            </div>
            <div class="w-full bg-gray-200 p-1 rounded-lg">
                <app-button text="Login" [isFormSelected]="isLoginSelected()" (selectForm)="selectLogin()" />
                <app-button text="Registro" [isFormSelected]="isRegisterSelected()" (selectForm)="selectRegister()" />
            </div>
            <div>
                @if (isLoginSelected()) {
                <app-login-form />
                } @else if (isRegisterSelected()) {
                <app-register-form />
                }
            </div>
        </div>
    </div>
</div>
