import { PropertyStatusTranslatePipe } from './property-status-translate-pipe';

describe('PropertyStatusTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyStatusTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
